# Usuários e RBAC

Esta seção consolida os perfis do produto, o respectivo escopo de atuação e as permissões-base esperadas no MVP.

## Matriz base de perfis


| ID             | Perfil                   | Escopo                                            | Permissões-base                                                                                                                                                                                                                                                                                                  | SPEC                                                                                                                    |
| -------------- | ------------------------ | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `REQ-RBAC-001` | `SuperAdmin`             | `cross-tenant`                                    | Visão transversal de toda a plataforma e gestão global de acesso.                                                                                                                                                                                                                                                | [04-rbac-permissoes.md](../SPEC/04-rbac-permissoes.md#perfis-de-acesso)                                                 |
| `REQ-RBAC-002` | `Board`                  | `cross-tenant`                                    | Acesso analítico e executivo a dashboards e comparativos entre obras, limitado a leitura.                                                                                                                                                                                                                        | [04-rbac-permissoes.md](../SPEC/04-rbac-permissoes.md#regras-transversais-de-isolamento-e-bypass)                       |
| `REQ-RBAC-003` | `AdminOperacional`       | Obra(s) explicitamente atribuída(s)               | Gestão operacional da obra, incluindo criação de demandas atípicas, alocação manual de operador e administração de cadastros operacionais.                                                                                                                                                                       | [04-rbac-permissoes.md](../SPEC/04-rbac-permissoes.md#matriz-completa-de-permissoes-por-recurso)               |
| `REQ-RBAC-004` | `UsuarioInternoFGR`      | Obra(s) explicitamente atribuída(s)               | Perfil para **Gerentes, Engenheiros e Encarregados de Obra**. Acesso ao painel web completo do módulo Machinery-Link com visibilidade gerencial total (leitura de todos os recursos). Acesso ao aplicativo mobile para criação de demandas simples (view equivalente ao `Empreiteiro`, sem pré-seleção de operador). Pode atualizar metadados de demandas existentes. Pode **criar agendamentos futuros**, que nascem no estado `AGUARDANDO_APROVACAO` e só transitam para `AGENDADA` após aprovação explícita de `AdminOperacional` ou `SuperAdmin` (DEC-027). **Não pode:** cancelar demandas, redistribuir ou realocar demandas, antecipar ou cancelar demandas agendadas, devolver demandas em execução, nem gerir cadastros operacionais (usuários, maquinários, tipos de serviços). (DEC-020) → SPEC: [04-rbac-permissoes.md](../SPEC/04-rbac-permissoes.md#matriz-completa-de-permissoes-por-recurso) · [03-fila-scoring-estados-sla.md](../SPEC/03-fila-scoring-estados-sla.md) |
| `REQ-RBAC-005` | `Empreiteiro`            | Contexto do próprio trabalho na obra atribuída    | Abertura de chamados e visibilidade restrita às suas demandas, sem acesso às filas de outros empreiteiros. Leitura de contexto auxiliar (obra, hierarquia territorial, catálogo de serviços e materiais) é permitida na medida necessária para preencher o formulário de abertura de demanda.                    | [04-rbac-permissoes.md](../SPEC/04-rbac-permissoes.md#matriz-completa-de-permissoes-por-recurso)               |
| `REQ-RBAC-006` | `Operador de Maquinário` | Contexto do próprio expediente e fila operacional | Acesso exclusivamente às demandas otimizadas/direcionadas na interface mobile, com prioridades máximas em destaque no topo. Leitura de contexto auxiliar (obra, setor operacional, quadra, lote, catálogo de maquinário e serviços) é permitida na medida necessária para visualizar a fila e executar demandas. Para demandas agendadas compatíveis (por `TipoMaquinario`): pode **aceitar** (fica vinculado à demanda) ou **recusar** (demanda permanece na aba "Demandas Agendadas" com log `RECUSADA`). Se `operadorAlocadoId` estiver definido, o fluxo de aceite é ignorado (bypass direto). Operador não pode aceitar mais de uma demanda no mesmo slot horário (janela de conflito configurável por obra). Para demandas agendadas já aceitas: pode **solicitar cancelamento**, que fica pendente de aprovação do `AdminOperacional`/`SuperAdmin`. (DEC-026, DEC-029) → SPEC: [04-rbac-permissoes.md](../SPEC/04-rbac-permissoes.md#matriz-de-permissoes-condicionadas-ao-estado-da-demanda) |


## Princípios de autorização do produto

> **Nota de nomenclatura:** Neste documento, os termos `Operador de Maquinário` (nome completo usado na matriz base) e `Operador` (nome curto) são sinônimos oficiais e referem-se ao mesmo perfil (`REQ-RBAC-006`). A SPEC adota o nome curto `Operador` como identificador canônico.

- A plataforma adota uma matriz rígida de perfis e permissões; capacidades administrativas e operacionais não são inferidas fora das regras explícitas do papel atribuído.
- O escopo por obra é a regra padrão. Apenas perfis `cross-tenant` podem operar fora do isolamento por obra.
- O perfil `Board` existe para leitura analítica e comparativa, não para execução operacional nem mutações de dados.
- O perfil `AdminOperacional` pode gerir uma ou várias obras, mas sempre por atribuição explícita.
- O perfil `UsuarioInternoFGR` tem acesso dual: painel web gerencial (leitura total do Machinery-Link) e aplicativo mobile para criação de demandas simples e agendamentos futuros (que nascem em `AGUARDANDO_APROVACAO` — DEC-027). Não possui poderes de cancelamento, redistribuição, realocação ou gestão de cadastros.
- O perfil `Operador` utiliza uma experiência mobile focada na execução, e não um painel geral de administração.

## Critérios de aceite relacionados

- [REQ-ACE-001](05-criterios-aceite.md#isolamento-rbac-e-multi-tenancy)
- [REQ-ACE-008](05-criterios-aceite.md#isolamento-cross-tenant-auditado)

